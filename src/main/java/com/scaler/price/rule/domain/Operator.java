package com.scaler.price.rule.domain;

public enum Operator {
    EQUALS,
    NOT_EQUALS,
    GREATER_THAN,
    LESS_THAN,
    GREATER_THAN_EQUALS,
    LESS_THAN_EQUALS,
    IN,
    NOT_IN,
    BETWEEN,
    CONTAINS,
    STARTS_WITH,
    ENDS_WITH,
    BEFORE,
    AFTER,
    WITHIN_PERCENTAGE
}
