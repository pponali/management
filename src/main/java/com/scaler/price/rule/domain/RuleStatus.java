package com.scaler.price.rule.domain;

public enum RuleStatus {
    ACTIVE,
    INACTIVE,
    DRAFT,
    SCHEDULED,
    EXPIRED,
    ARCHIVED,
    SUSPENDED,
    PENDING_APPROVAL,
    APPROVED,
    REJECTED
}
